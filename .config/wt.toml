# Worktrunk project config — shared by all contributors
# See: https://worktrunk.dev

[pre-start]
# See: .envrc
direnv = "direnv allow"

[[post-start]]
# See: .worktreeinclude
copy = "wt step copy-ignored"

[[post-start]]
deps = "pnpm install"

[[pre-commit]]
lint = "pnpm lint"
typecheck = "pnpm astro check"

[[pre-merge]]
build = "pnpm build"
generate-cv = "pnpm generate:cv"

